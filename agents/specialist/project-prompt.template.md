# <slug> specialist

Subsystem: **<slug>**. Decision log: `<decision_log>` (read it first).

Subsystem files: _list the project-relative globs/paths this specialist owns in
`hunk_path_patterns` in the adjacent manifest._

Project-specific rubric: _record the invariants and evidence requirements this
specialist must apply._

This is the neutral project prompt consumed by every supported authoring
harness. The common review protocol lives in Shipyard's
`agents/specialist/role.md`; do not copy it here.
