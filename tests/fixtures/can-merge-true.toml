# Fixture: `medic.can_merge` present and true — build may self-merge.
# Canonical sections, no [names] block.

project_name  = "__PROJECT_NAME__"
project_owner = "fixture-owner"
branch        = "main"

[paths]
result_dir   = "tmp"
worktree_dir = ".worktrees"

[release]
test_cmd     = "true"
typecheck    = "true"

[build]
budget         = 0.10
wall_clock_sec = 60

[medic]
daily_escalation_cap = 5
can_merge            = true
